import 'package:flutter/material.dart';

class ClinicAuth extends StatelessWidget {
  const ClinicAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
